export declare class Folder {
	constructor(
		organizationId: string,
		name: string,
		description: string,
		createdAt: Date,
		updatedAt: Date,
		projectId: string
	);

	organizationId: string;
	name: string;
	description: string;
	createdAt: Date;
	updatedAt: Date;
	projectId: string;
}
